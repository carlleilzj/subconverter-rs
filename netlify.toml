[build]
base = "www"
command = "npm install --include=dev && npm run build"
publish = ".next"

[[plugins]]
package = "@netlify/plugin-nextjs"

[build.environment]
NODE_VERSION = "22"
NODE_ENV = "production"
